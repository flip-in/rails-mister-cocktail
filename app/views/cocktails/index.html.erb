<div class="container">
  <div class="banner">
  <div class="container">
    <h1>Happy Hour Cocktails</h1>
    <p>What will you drink at sunset tonight?</p>
    <a class="btn btn-flat" href="#">Browse now</a>
  </div>
</div>
  <% @cocktails.each do |cocktail| %>
    <p>
      <%= link_to cocktail do %>
        <%= cocktail.name %>
      <% end %>
    </p>
  <% end %>
  <%= link_to "Add a new Cocktail", new_cocktail_path, class: "btn btn-primary"%>
</div>

